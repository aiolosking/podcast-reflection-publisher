# 使用说明 · podcast-reflection-publisher

配套文件：流程定义见 `SKILL.md`，细则见 `references/`，输入清单见 `templates/task-brief.md`。
这份文档只回答一件事：**下一次你要出稿时，具体该怎么做。**

---

## 1. 开工前先备齐两样东西

| 必需 | 具体要求 |
| --- | --- |
| 一期播客 | 三选一：音频文件（mp3 / m4a 等，放在工作目录里最省事）、节目网页链接（小宇宙 / Apple Podcasts 等）、你已经拿到的逐字稿 |
| 作者风格报告 | 一份**已经总结好**的文档，JSON / Markdown / TXT 均可。要写到"怎么开头、句子长短、小节怎么断、怎么收尾、常用什么修辞、有什么禁忌"这个颗粒度；只给一句"学某某的文风"是不够的 |

可选但很有用：你自己听完真想说的几句话（原话即可，不用组织成文）、指定切入角度、目标读者、字数要求（默认 2500–3500 字）、你自己手里的真实照片、指定发布平台。

风格报告不内置、也不会替你分析样本——**上传谁的报告，出来就是谁的风格**。想换文风就换一份报告，其余流程不变。

---

## 2. 触发：照抄这句话就行

> 这是这期播客的音频（或链接），这是作者风格报告。帮我写一篇公众号听后感：要真实配图，出可以双击打开的本地 HTML，然后发布成别人免登录能看的链接。字数 2500–3500，读者是普通科技/商业读者。我想重点分享的是：×××。

不写"我想分享什么"也没关系，但要写清楚**分享身份**——是"我们听完想把这个故事讲给你"，还是"我个人的私货"。这一句决定标题和视角，事后返工成本最高。

---

## 3. 流程会走八个阶段，各阶段要不要你动手

**阶段 0 准备。** 建工作目录：`article/`（成稿）、`images/`（原图）、`sources.md`（事实与图片来源）、`内部资料/`（转写与核对记录，禁止发布）。不用你动手。

**阶段 1 理解音频。** 检查格式与时长，取全文字内容，长音频分段处理并保留时间位置，听不清的人名/数字/术语标为"不确定"而不擅自补成事实。产出节目框架、关键故事、主要观点、分歧、待核实清单。
→ **可能需要你点头**：音频要上传到外部转写服务，或需要付费调用。缺授权时会先说明用途和费用，不会自己掏钱。

**阶段 2 套用风格。** 完整读你给的报告，按它落笔；不重新分析样本；不把风格来源作者的身份、经历、署名搬进正文。不用你动手。

**阶段 3 写作。** 给 3 个标题候选，产出原创文章——不是逐字稿压缩、不是章节摘要、不是人物年表。查不到的信息标明"节目转述、未逐一外部核实"，不虚构"我采访了""我实测过"。
→ **建议在这里停一次**：确认标题和切入角度。

**阶段 4 配图。** 3–5 张**真实**图片，逐张实际看过再决定用不用，记录来源页、作者、授权和图注，资料照会标"资料照"。禁止 AI 生图、SVG 示意图、虚构截图、占位图。不用你动手，但图片清单会给你过目。

**阶段 5 本地 HTML。** 单栏、电脑手机都能读、样式与图片全部内嵌、断网也正常显示；同时留 `article.md` 和 `images/` 原始文件。不用你动手。

**阶段 6 发布。** 默认走匿名免账号的临时直传（见第 5 节）。
→ **需要授权**：确认允许公开，确认接受临时链接的限制。你说"只要本地稿"时这一步直接跳过。

**阶段 7 验证与交付。** 实际打开 HTML 看排版和图、核对中文与链接、在未登录状态访问公开链接。最后报：本地文件位置、公开链接、切入角度一句话、以及**没验证成功的项**。没实跑过的环节会如实写，不会声称全部通过。

---

## 4. 四个必须你点头的节点

1. 音频上传给外部转写服务 / 任何付费调用
2. 文章是否公开发布
3. 是否接受临时链接（会过期、不可删除、不可续期）
4. 任何要动你账号或凭据的操作

其余情况它不该反复问你。同一件已授权的事不会二次确认。

---

## 5. 拿到成品后，先自己查这六项

1. 标题和视角是不是你要的分享身份
2. 正文里有没有把你没做过的事写成你做的
3. 文末三段落：原始播客链接、配图来源、版权说明
4. 双击 HTML，断网状态下 7 张图是不是都在
5. 公开链接在你手机流量（换个网络）下能不能打开
6. 字数和阅读时长是否合适

---

## 6. 关于发布：临时床与永久链接

默认路线是匿名免账号直传，优点是零配置零成本，缺点是三条硬限制：**约 72 小时后失效**、**不能删除也不能续期**、**不保证在你当前网络可达**（本项目实测过：同一地址从本机超时打不开，从另一条网络路径能正常抓到正文）。

要长期地址，两条路，都需要你本人的账号：

- **GitHub Pages**（已有本仓库，最省事）：在仓库 Settings → Pages 开启，之后文章放仓库子目录，地址形如 `https://<用户名>.github.io/<仓库>/<文章>.html`。
- **Surge / Netlify**：链接稳定、可删可换，但首次要注册和登录。

涉及账号凭据的操作，一律是：**我写好不含密钥的脚本，你本机执行**（保存 token 文件 → 双击 `.bat` → 回来告诉我跑完了），我全程不复述、不落盘、不进 git 配置。用完你删文件、在网页上 Revoke token。

---

## 7. 这套流程上一次的实测注意点（写给下一次）

- **git / curl 连不上 github.com**（TLS 被中途重置），但 Python 直连可用 → 上传走 GitHub REST API。
- **空仓库不能直接用 Git Data API**（返回 409 "Git Repository is empty"）→ 先用 Contents API 建第一个文件，再走 blob/tree/commit 一次成提交。
- 上传完要做**公开侧回读比对**（`raw.githubusercontent.com` 逐文件 md5），不能只看接口返回 200。
- 本机 `python` 是商店占位程序，要用 `scoop` 里那个完整路径；控制台是 GBK，中文打印会乱码 → 中文写文件再读，屏幕上只打 ASCII 指标。
- 密钥一旦粘进对话，除了本机文件还会留在应用自己的会话记录里；撤销是唯一真正有效的动作，所以**先撤销再收尾**。
- 免费图床/匿名直传稳定性差，重要链接不要只留一份。

---

## 8. 常见情况怎么处理

**换一位作者的风格。** 只换风格报告，其余不用改。
**只要本地稿。** 明说"先不发"，阶段 6 整体跳过。
**音频很长。** 分段处理、保留时间位置；若环境里没有可用转写能力，会直说并请补文字稿，不会假装听完。
**图片想自己给。** 连同素材一起给，并说明来源与可用性；Skill 只做筛选与图注，不会伪造来源。
**想改标题或身份。** 越早说越省——它影响的不只是标题行，还有副标题、开头视角和收尾语气。
**要发布到已有站点。** 把站点信息和已有的登录态交出来（或你本机执行认证），Skill 按 `references/publishing.md` 的优先级改用它，不再走临时床。

---

## 9. 仓库里哪个文件管什么

`SKILL.md` 主流程与硬性边界 · `references/writing-rules.md` 风格套用与写作要求 · `references/image-rules.md` 真实配图要求 · `references/publishing.md` 发布探测与隐私边界 · `scripts/publish_anonymous.py` 默认匿名上传 · `templates/task-brief.md` 输入收集与授权确认 · 本文 `USAGE.md` 操作说明。
